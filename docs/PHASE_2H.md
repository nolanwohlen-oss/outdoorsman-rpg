# Phase 2H — physical inventory

Phase 2H replaces placeholder quantities with a bounded physical inventory: defined item types, item weights, carry capacity, and atomic add/remove operations. Raw retained fish is stored separately from prepared ration calories. The next camping layer will consume and transform these items.
